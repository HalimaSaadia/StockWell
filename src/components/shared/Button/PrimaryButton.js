import "./primaryButton.css"

const PrimaryButton = () => {
  return (
    <button type="submit" className="w-full">
      <span className="text font-bold">BUTTON</span>
    </button>
  );
};

export default PrimaryButton;
