import LoginPage from '@/components/ui/loginPage/LoginPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <LoginPage />
        </div>
    );
};

export default page;